<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ArenillasTurismo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryDarkColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
        <item name="colorSurface">@color/primaryTextColor</item>
        <item name="colorOnSurface">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryDarkColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="SplashScreenTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowBackground">?attr/colorPrimary</item>
    </style>

    <style name="CardStyleButton">
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="cardElevation">8dp</item>
        <item name="cardCornerRadius">@dimen/rounded_card_button</item>
        <item name="cardBackgroundColor">?attr/colorSecondary</item>
    </style>

    <style name="SliderImageStyle">
        <item name="sliderIndicatorAnimationDuration">600</item>
        <item name="sliderIndicatorGravity">center_horizontal|bottom</item>
        <item name="sliderIndicatorMargin">15dp</item>
        <item name="sliderIndicatorOrientation">horizontal</item>
        <item name="sliderIndicatorPadding">3dp</item>
        <item name="sliderIndicatorRadius">2dp</item>
        <item name="sliderIndicatorSelectedColor">@color/secondaryTextColor</item>
        <item name="sliderIndicatorUnselectedColor">@color/secondaryDarkColor</item>
        <item name="sliderScrollTimeInSec">3</item>
        <item name="sliderStartAutoCycle">true</item>
        <item name="sliderAnimationDuration">600</item>
        <item name="sliderAutoCycleDirection">back_and_forth</item>
        <item name="sliderAutoCycleEnabled">true</item>
    </style>

    <style name="ButtonArStyle">
        <item name="android:text">@string/button_go_to_ar</item>
        <item name="android:textSize">18sp</item>
        <item name="icon">@drawable/icon_camera_24</item>
        <item name="iconGravity">end</item>
        <item name="iconSize">25dp</item>
    </style>

    <style name="Widget.App.Button.OutlinedButton.IconOnly" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="iconPadding">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:minWidth">48dp</item>
        <item name="android:minHeight">48dp</item>
    </style>

</resources>